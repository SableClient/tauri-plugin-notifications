<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <intent>
            <action android:name="org.unifiedpush.android.distributor.REGISTER" />
        </intent>
    </queries>

    <application>
        <receiver android:name="app.tauri.notification.TimedNotificationPublisher" android:exported="true" />
        <receiver android:name="app.tauri.notification.NotificationDismissReceiver" android:exported="false" />
        <receiver
            android:name="app.tauri.notification.LocalNotificationRestoreReceiver"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Firebase Cloud Messaging Service for handling push notifications -->
        <service
            android:name="app.tauri.notification.TauriFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- UnifiedPush connector push service. The connector library's
             internal MessagingReceiverImpl receives distributor broadcasts
             and forwards them to this service, so no BroadcastReceiver is
             declared for the connector actions. -->
        <service
            android:name="app.tauri.notification.UnifiedPushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.unifiedpush.android.connector.PUSH_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name="app.tauri.notification.PushWorkForegroundService"
            android:exported="false"
            android:foregroundServiceType="shortService" />

        <!-- Embedded UnifiedPush distributor: holds the gateway websocket open when
             neither FCM nor a third-party distributor can deliver. -->
        <service
            android:name="app.tauri.notification.EmbeddedPushService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

        <receiver
            android:name="app.tauri.notification.EmbeddedPushBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />
</manifest>